<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Vista Form del Wizard de Costos -->
        <record id="view_broiler_flock_cost_wizard_form" model="ir.ui.view">
            <field name="name">broiler.flock.cost.wizard.form</field>
            <field name="model">broiler.flock.cost.wizard</field>
            <field name="arch" type="xml">
                <form string="Actualizar Costos del Lote">
                    <group>
                        <field name="flock_id" readonly="1"/>
                        <field name="cost_description"/>
                        <field name="cost_amount"/>
                        <field name="cost_type"/>
                    </group>
                    <footer>
                        <button string="Actualizar Costo" name="action_update_cost" type="object" class="btn-primary"/>
                        <button string="Cancelar" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <!-- Acción del Wizard -->
        <record id="action_broiler_flock_cost_wizard" model="ir.actions.act_window">
            <field name="name">Actualizar Costos del Lote</field>
            <field name="res_model">broiler.flock.cost.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

    </data>
</odoo>